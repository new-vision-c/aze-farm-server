<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>Code de Vérification</title>
    <style>
      body {
        font-family: 'Segoe UI', Roboto, Arial, sans-serif;
        background-color: #f8f9fb;
        margin: 0;
        padding: 0;
        color: #333;
      }

      .container {
        max-width: 600px;
        margin: 40px auto;
        background-color: #fff;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.1);
      }

      .header {
        background-color: #007bff;
        background-image: linear-gradient(120deg, #007bff, #0056b3);
        padding: 30px;
        text-align: center;
        color: white;
      }

      .content {
        padding: 30px;
        text-align: center;
      }

      .otp-code {
        font-size: 36px;
        font-weight: bold;
        color: #007bff;
        background-color: #f8f9fa;
        padding: 20px;
        border-radius: 8px;
        margin: 20px 0;
        letter-spacing: 4px;
        border: 2px dashed #007bff;
      }

      .title {
        font-size: 24px;
        font-weight: 600;
        margin-bottom: 10px;
        color: #333;
      }

      .message {
        font-size: 16px;
        line-height: 1.6;
        color: #666;
        margin-bottom: 20px;
      }

      .footer {
        background-color: #f8f9fa;
        padding: 20px;
        text-align: center;
        font-size: 12px;
        color: #666;
      }

      .warning {
        color: #dc3545;
        font-weight: 500;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>🔐 Code de Vérification</h1>
      </div>

      <div class="content">
        <div class="title">
          Bonjour <%= name %>,
        </div>

        <div class="message">
          Voici votre code de vérification pour valider votre compte :
        </div>

        <div class="otp-code">
          <%= content %>
        </div>

        <div class="message">
          Ce code expirera dans <strong>10 minutes</strong>.
        </div>

        <div class="message warning">
          Si vous n'avez pas demandé ce code, merci d'ignorer cet email.
        </div>
      </div>

      <div class="footer">
        <p>Cet email a été envoyé automatiquement. Merci de ne pas y répondre.</p>
      </div>
    </div>
  </body>
</html>